#include "holberton.h"

/**
 * _strlen - calcuate the lenght of a string
 * @s: Array of strings
 * Return: a length of *s
*/
int _strlen(char *s)
{
	int count = 0;

	while (s[count] = !0)
		count++;
	return count;
}
